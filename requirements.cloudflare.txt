# Veilcrean — production requirements for Cloudflare Containers
# This removes test/dev-only packages from requirements.txt to keep the image smaller.
# Torch is installed with the CPU wheel index in Dockerfile.cloudflare.

# Core scientific stack
numpy>=1.26.0,<3
pandas>=2.1.0,<3
scipy>=1.11.0

# Machine learning
torch>=2.1.0
scikit-learn>=1.3.0

# Data sources / technical indicators
# (ta / pandas_ta were listed historically but nothing imports them; both
#  are uninstallable on Python 3.11, so they are intentionally omitted.)

# Communication
pyzmq>=25.1.0
msgpack>=1.0.7
websockets>=12.0

# Database
sqlalchemy>=2.0.23

# Visualization / dashboard utilities used by the project
matplotlib>=3.8.0
plotly>=5.17.0
dash>=2.14.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# LLM support
google-generativeai>=0.3.0
groq>=0.3.0

# Alerts / HTTP
requests>=2.31.0
python-telegram-bot>=20.7

# Logging / utilities
loguru>=0.7.0
tqdm>=4.66.0
python-dateutil>=2.8.2
pytz>=2023.3
